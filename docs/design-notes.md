# 展示设计与参考

这轮升级把静态介绍延伸为可操作的工程作品集，而非复制真实交易终端。

## 学习的作品集

参考日期：2026-09-25。仅学习信息组织方式，页面、样式和演示代码均独立编写；没有复制模板代码、图片或个人文案。

| 参考 | 学习点 | 在本项目中的应用 |
| --- | --- | --- |
| [Brittany Chiang v4](https://github.com/bchiang7/v4) / [网站](https://v4.brittanychiang.com/) | 首屏定位明确，项目与经历有清晰层级，行动入口醒目 | 首屏先说明项目价值，紧接可体验的工作台，再讲工程取舍 |
| [DeveloperFolio](https://github.com/saadpasta/developerFolio) / [网站](https://developerfolio.js.org/) | 分区展示技能与项目，提供项目访问入口，支持响应式布局 | 区分交互演示、架构、工程案例与公开边界，适配移动端 |
| [Lee Robinson](https://leerob.com/) | 简洁的个人定位和分层内容入口 | 避免无依据的能力评分与成果数字，把篇幅用于可解释的设计 |

## 为什么不是更长的 README

GitHub README 适合快速浏览，但不运行自定义 JavaScript。交互体验放在同一公开仓库的 GitHub Pages，README 保留项目摘要和演示入口。

## 三个合成演示

1. **决策回放：** 正常流程、响应超时、行情过期三种分支；支持单步、自动回放、暂停与重置。
2. **异常对账：** 重复请求不会增加模拟提交次数；补充合成证据后，状态从 unknown 收敛为 reconciled。
3. **数据闸门：** 调整合成快照年龄，观察“数据仍显示”与“新执行被拦截”的区别。60 秒仅为演示阈值。

另有六个可点击架构模块和可展开的工程案例。所有交互都在浏览器内存中运行，刷新后恢复初始状态。

## 视觉与技术选择

- 使用暖灰底、深绿工作台和低饱和青柠色，保持研究工具的克制感。
- 页面优先使用中文，英文仅用于辅助标签和工程状态。
- 使用系统字体、自有 SVG 和原生 HTML/CSS/JavaScript；无 CDN、分析脚本和第三方字体。
- 核心内容在关闭 JavaScript 时仍可阅读；交互使用原生按钮、选择器、滑块和折叠元素。
- 展示页使用独立状态机，不导入私有项目源码，不读取任何账户快照。

## 本地检查

在本仓库根目录运行：

```sh
node --test tests/demo-model.test.mjs
python3 scripts/check_public.py
python3 -m http.server 8769 --bind 127.0.0.1 --directory docs
```

前两项检查演示状态边界和公开文件白名单。它们验证的是作品集本身，不是交易平台的全量回归。
